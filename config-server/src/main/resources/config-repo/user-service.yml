server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/demo?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: marcoreus
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.example.fintech.userservice: INFO